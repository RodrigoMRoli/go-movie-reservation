version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "movie_resevation"
        out: "movie_resevation"
        emit_json_tags: true
        emit_interface: true
